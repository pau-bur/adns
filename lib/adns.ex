defmodule Adns do
  @moduledoc """
  Elixir DNS codec, concurrent UDP client/server, and pluggable resolvers (RFC 1035).

  See the project README for architecture, examples, and benchmarks. Primary entry
  points: `Adns.Message`, `Adns.Server.UDP`, `Adns.Client`, and `Adns.Resolver`.
  """
end
